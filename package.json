{
  "name": "alfred",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "bundle": "node scripts/bundle.mjs",
    "setup": "node packages/cli/dist/index.js setup",
    "prepublish": "pnpm build && pnpm bundle"
  },
  "pnpm": {
    "overrides": {
      "form-data": "^4.0.5"
    }
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "esbuild": "^0.24.0",
    "prettier": "^3.4.0",
    "turbo": "^2.4.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
